info:
  name: driver
  type: http

http:
  method: GET
  url: http://127.0.0.1:5000/driver
  body:
    type: json
    data: |-
      {
        "name": "Mark Smith",
        "license_type": "CDL"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
